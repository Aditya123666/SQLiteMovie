<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harun.voting_fix">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".first_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".Home"></activity>
        <activity android:name=".tutormain"></activity>
        <activity android:name=".vottingmain"></activity>
        <activity android:name=".votenext"></activity>
        <activity android:name=".votenext1"></activity>
        <activity android:name=".regismain"></activity>
        <activity android:name=".main_quick"></activity>
        <activity android:name=".admin_login"></activity>
    </application>

</manifest>